{
  "name": "conveyor-sim",
  "version": "0.1.0",
  "description": "Motor y render reutilizables para simulación de transportadores (base para un modelador online).",
  "type": "module",
  "scripts": {
    "test": "node test/harness.mjs && node test/catalog.mjs",
    "build": "node build/standalone.mjs",
    "gif": "node build/render-gif.mjs",
    "serve": "http-server . -p 8080"
  },
  "dependencies": {
    "three": "0.158.0"
  },
  "devDependencies": {
    "omggif": "^1.0.10"
  },
  "license": "MIT"
}
